# Copy to secrets.zsh (gitignored) and fill in. Sourced from .zshrc.
#   cp secrets.zsh.example secrets.zsh && chmod 600 secrets.zsh
#
# Never put these in .zshrc — that file is tracked in a PUBLIC repo.

# export GEMINI_API_KEY="..."
# export UV_INDEX_AZURE_PASSWORD="..."
